import { UserDetailCardModel } from './user-detail-card-model';

describe('UserDetailCardModel', () => {
  it('should create an instance', () => {
    expect(new UserDetailCardModel()).toBeTruthy();
  });
});
